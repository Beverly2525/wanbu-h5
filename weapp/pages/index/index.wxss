.page {
  min-height: 100vh;
  padding: 0 32rpx 56rpx;
  background: #f5f5f7;
}

button {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

button::after {
  border: 0;
}

.hero {
  margin: 0 -32rpx;
  padding: 42rpx 32rpx 44rpx;
  color: #f7fff9;
  background: linear-gradient(180deg, #2f8a63 0%, #17392f 100%);
}

.hero-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
}

.eyebrow,
.pool-label {
  color: rgba(255, 255, 255, 0.68);
  font-size: 24rpx;
  font-weight: 600;
}

.title {
  margin-top: 8rpx;
  font-size: 58rpx;
  font-weight: 800;
  letter-spacing: 0;
  line-height: 1.05;
}

.status-dot {
  min-width: 104rpx;
  height: 48rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.14);
  color: rgba(255, 255, 255, 0.86);
  font-size: 24rpx;
  font-weight: 700;
  line-height: 48rpx;
  text-align: center;
}

.ring {
  display: flex;
  width: 360rpx;
  height: 360rpx;
  margin: 42rpx auto 32rpx;
  align-items: center;
  justify-content: center;
  border: 20rpx solid rgba(152, 223, 178, 0.95);
  border-radius: 50%;
  box-shadow: inset 0 0 0 18rpx rgba(255, 255, 255, 0.12);
}

.ring-inner {
  text-align: center;
}

.steps {
  font-size: 72rpx;
  font-weight: 850;
  line-height: 1;
}

.target {
  margin-top: 14rpx;
  color: rgba(255, 255, 255, 0.72);
  font-size: 25rpx;
}

.stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18rpx;
}

.stats > view {
  padding: 22rpx 24rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.18);
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(20rpx);
}

.stat-value,
.stat-label,
.pool-meta text {
  display: block;
}

.stat-value {
  font-size: 38rpx;
  font-weight: 800;
}

.stat-label {
  margin-top: 6rpx;
  color: rgba(255, 255, 255, 0.68);
  font-size: 24rpx;
}

.panel,
.pool,
.rules,
.row,
.empty {
  border-radius: 26rpx;
  background: rgba(255, 255, 255, 0.94);
  box-shadow: 0 16rpx 44rpx rgba(30, 38, 34, 0.08);
}

.panel {
  margin-top: -26rpx;
  padding: 34rpx;
}

.panel-title,
.section-title {
  color: #111814;
  font-size: 34rpx;
  font-weight: 800;
}

.panel-copy {
  margin-top: 10rpx;
  color: #747b76;
  font-size: 25rpx;
  line-height: 1.48;
}

.nickname {
  height: 92rpx;
  margin-top: 28rpx;
  padding: 0 26rpx;
  border: 0;
  border-radius: 22rpx;
  background: #f4f5f4;
  color: #111814;
  font-size: 29rpx;
}

.stake-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8rpx;
  margin: 24rpx 0;
  padding: 8rpx;
  border-radius: 24rpx;
  background: #f1f3f1;
}

.stake {
  display: flex;
  height: 76rpx;
  align-items: center;
  justify-content: center;
  border-radius: 18rpx;
  background: transparent;
  color: #111814;
  font-size: 30rpx;
  font-weight: 800;
}

.stake.active {
  background: #fff;
  color: #26724f;
  box-shadow: 0 8rpx 20rpx rgba(28, 45, 36, 0.08);
}

.primary,
.secondary {
  height: 92rpx;
  border-radius: 22rpx;
  font-size: 30rpx;
  font-weight: 800;
  line-height: 92rpx;
}

.primary {
  background: #2f8a63;
  color: #fff;
  box-shadow: 0 16rpx 32rpx rgba(47, 138, 99, 0.22);
}

.secondary {
  margin-top: 14rpx;
  border: 1rpx solid rgba(17, 24, 20, 0.1);
  background: #fff;
  color: #1d2924;
}

.pool {
  display: flex;
  margin-top: 28rpx;
  padding: 32rpx;
  align-items: center;
  justify-content: space-between;
  background: #151d19;
  color: #fff;
}

.pool-amount {
  margin-top: 8rpx;
  font-size: 56rpx;
  font-weight: 850;
  line-height: 1;
}

.pool-meta {
  display: grid;
  gap: 12rpx;
  color: rgba(255, 255, 255, 0.72);
  font-size: 25rpx;
  text-align: right;
}

.board {
  margin-top: 34rpx;
}

.rules {
  margin-top: 28rpx;
  padding: 32rpx;
}

.rule-card {
  display: flex;
  gap: 20rpx;
  margin-top: 22rpx;
  padding: 24rpx;
  border-radius: 22rpx;
  background: #f4f6f4;
}

.rule-index {
  display: flex;
  width: 52rpx;
  height: 52rpx;
  flex: 0 0 52rpx;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #e4f1e9;
  color: #26724f;
  font-size: 22rpx;
  font-weight: 850;
}

.rule-copy {
  min-width: 0;
}

.rule-title {
  color: #111814;
  font-size: 27rpx;
  font-weight: 800;
}

.rule-text {
  margin-top: 8rpx;
  color: #626a65;
  font-size: 25rpx;
  line-height: 1.5;
}

.row {
  display: grid;
  grid-template-columns: 58rpx 1fr auto;
  gap: 16rpx;
  align-items: center;
  margin-top: 18rpx;
  padding: 24rpx;
}

.rank {
  width: 50rpx;
  height: 50rpx;
  border-radius: 50%;
  background: rgba(47, 138, 99, 0.1);
  color: #26724f;
  font-size: 23rpx;
  font-weight: 800;
  line-height: 50rpx;
  text-align: center;
}

.rank.gold,
.rank.silver,
.rank.bronze {
  background: transparent;
  font-size: 34rpx;
}

.person {
  min-width: 0;
}

.name {
  overflow: hidden;
  color: #111814;
  font-size: 29rpx;
  font-weight: 800;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.meta {
  margin-top: 6rpx;
  color: #7a817d;
  font-size: 24rpx;
}

.done {
  color: #26724f;
  font-size: 24rpx;
  font-weight: 800;
}

.done.risk {
  color: #d45a4c;
}

.empty {
  margin-top: 18rpx;
  padding: 28rpx;
  color: #7a817d;
  font-size: 26rpx;
  text-align: center;
}
